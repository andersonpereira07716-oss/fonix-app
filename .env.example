# FASE 2 — copie para .env e preencha com os dados do seu projeto Supabase.
# Nunca use a service_role key aqui — apenas a anon key pública (protegida por RLS).
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key-publica
